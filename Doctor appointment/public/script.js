async function addPatient() {
    const firstName = document.getElementById('firstName').value;
    const lastName = document.getElementById('lastName').value;
    const age = document.getElementById('age').value;
    const gender = document.getElementById('gender').value;
  
    // Call the add_patient function in your smart contract
    // and pass the patient details as arguments
    // Use AE SDK or other means to interact with the contract
  }
  
  async function getPatient() {
    const patientId = document.getElementById('patientId').value;
  
    // Call the get_patient function in your smart contract
    // and pass the patient ID as an argument
    // Use AE SDK or other means to interact with the contract
  }
  